services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./data:/data
    environment:
      ADMIN_TOKEN: "ChangeThisToAVeryLongRandomPassword"
      SIGNUPS_ALLOWED: "false"
