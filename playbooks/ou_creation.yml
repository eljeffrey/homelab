- name: Create AD OUs
  hosts: samba
  gather_facts: false

  vars:
    ous:
      - Servers
      - Workstations
      - groups
      - users	
  tasks:
    - name: Create OUs
      command: >
        samba-tool ou create "OU={{ item }},DC=home,DC=internal"
      loop: "{{ ous }}"
